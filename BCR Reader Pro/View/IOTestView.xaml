<Page x:Class="BCR_Reader_Pro.View.IOTestView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BCR_Reader_Pro.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="IOTestView"
      Background="White">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <!-- 왼쪽: Inputs -->
            <ColumnDefinition Width="*" />
            <!-- 오른쪽: Outputs -->
        </Grid.ColumnDefinitions>

        <!-- 왼쪽: Inputs -->
        <StackPanel Grid.Column="0">
            <TextBlock Text="Inputs"
                   FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,10"/>

            <ItemsControl ItemsSource="{Binding Inputs}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="8"/>
                        <!-- 열 개수 조절 가능 -->
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Name}"
                            Margin="3"
                            FontSize="12"
                            IsEnabled="False"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="DimGray"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding State}" Value="True">
                                            <Setter Property="Background" Value="LimeGreen"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- 오른쪽: Outputs -->
        <StackPanel Grid.Column="1">
            <TextBlock Text="Outputs"
                   FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,10"/>

            <ItemsControl ItemsSource="{Binding Outputs}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="8"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Name}"
                            Margin="3"
                            FontSize="12"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Command="{Binding DataContext.OutPutCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding Index}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="DimGray"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding State}" Value="True">
                                            <Setter Property="Background" Value="DodgerBlue"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>


</Page>
